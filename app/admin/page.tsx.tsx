export default function AdminHomePage() {
  return (
    <main
      style={{
        minHeight: "100vh",
        padding: 40,
        fontFamily: "system-ui",
      }}
    >
      <h1 style={{ fontSize: 32, fontWeight: 900 }}>Admin Home</h1>
      <p>If you can see this, the /admin route is working.</p>
    </main>
  );
}
